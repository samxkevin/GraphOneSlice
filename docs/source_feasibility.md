# AI Orbit Source Feasibility Findings

Generated from the latest executed AI Orbit pipeline run. This document records observed behavior only; unreachable sources are not represented as usable.

| Source | Domain | Access | Pagination | Required Fields | Freshness | Anti-bot/JS | Inventory Evidence | Status |
|---|---|---|---|---|---|---|---|---|
| GitHub API | Repositories/Companies | GitHub REST search/repository/org endpoints | page/per_page parameters; Link header appears when additional pages are available | full_name<br>html_url<br>description<br>owner<br>stargazers_count<br>language<br>updated_at | repository updated_at is available; it is not treated as publication time for news/jobs | GitHub REST API returned JSON; no browser automation or JavaScript required | search total_count=442 for configured query | usable |
| PyPI JSON API | Tools/Companies | PyPI project JSON endpoint /pypi/{project}/json | not paginated for a single project; releases are keyed by version | name<br>summary<br>project_url<br>project_urls<br>author<br>author_email<br>version<br>requires_python | version/release metadata exists, but package update time is not treated as product launch time | PyPI JSON API returned JSON; no browser automation or JavaScript required | configured packages=4; sample package=openai | usable |
| NPM Registry MCP Packages | MCP/Tools | NPM registry package document | not paginated for a single package document; versions are nested by version key | name<br>description<br>dist-tags.latest<br>versions[latest].bin<br>versions[latest].deprecated<br>readme | package version/update metadata exists, but not treated as product/news publication time | NPM registry JSON endpoint returned JSON; no browser automation or JavaScript required | configured MCP packages=4; sample package=@modelcontextprotocol/server-filesystem | usable |
| NPM Search AI Tool Packages | Tools/MCP/Creative | NPM registry search endpoint plus package-specific registry documents | search endpoint supports size/from; this adapter uses bounded configured query sizes | package.name<br>package.description<br>package.links.npm<br>package.version<br>package.date | package date/update fields are package version timestamps, not product launch or news publication times | NPM registry JSON endpoint returned JSON; no browser automation or JavaScript required | sample query 'keywords:openai' returned 1 object(s); total=10749 | usable |
| Official SDK Model Definitions | Models | GitHub REST contents API for official provider SDK model type files | not paginated; one GitHub contents response per SDK type file | provider SDK repository<br>source file content<br>model identifier literal | GitHub contents response reflects the current default branch at fetch time; model launch/publication time is not supplied | GitHub REST API returned JSON; no browser automation or JavaScript required | OpenAI: 83 identifiers; Anthropic: 15 identifiers | usable |
| AI Tools List Product Directory | Products | GitHub REST contents API for a JSON AI tools/product directory sample | sample JSON file is not paginated; full JSON file exists but is too large for GitHub contents base64 and raw.githubusercontent.com failed in this environment | id<br>handle<br>website<br>description | directory entries do not provide product launch or publication time; no freshness timestamp is exported for products | GitHub REST API returned JSON file content; no browser automation or JavaScript required | sample JSON rows=1001; candidate rows with required fields and explicit AI description evidence=836 | usable |
| Models.dev GitHub Model Catalog | Models | GitHub REST contents API for anomalyco/models.dev models.json | single generated JSON catalog; adapter uses a bounded deterministic sample rather than full ingestion | id<br>name<br>description<br>architecture.modality<br>architecture.input_modalities<br>architecture.output_modalities<br>links.details | records include a source 'created' epoch and optional knowledge_cutoff, but neither is treated as an independently verified model release date | GitHub REST API returned JSON file content; no browser automation or JavaScript required | models.json rows=364; candidate rows after required fields, alias/free-variant filtering, and OpenAI/Anthropic duplicate-avoidance=255 | usable |
| ROS Robots Catalog | Robots | GitHub REST contents API for ros-infrastructure/robots.ros.org _posts Markdown records | GitHub contents directory listing returns all _posts files for this repository path; adapter ingests a bounded deterministic sample | title<br>introduction or description/body<br>catalog post path<br>catalog URL or website/wiki URL | front-matter date is the robots.ros.org catalog post date; it is preserved as catalog_posted_at and not treated as robot launch date | GitHub REST API returned Markdown catalog files as JSON/base64; no browser automation or JavaScript required | post files listed=208; sampled post files=30; sampled usable robot records=29 | usable |
| GitHub Releases Announcements | News | GitHub REST releases API for a curated AI repository allowlist | per_page/page parameters; adapter ingests a bounded deterministic per-repository sample, round-robin across repositories | name or tag_name<br>html_url<br>published_at<br>body (release notes)<br>repository owner/login<br>repository AI relevance evidence | release published_at is the genuine GitHub publication timestamp for the announcement; it is preserved as news.published_at with explicit timestamp semantics and never substituted with crawl/commit time | GitHub REST API returned JSON; no browser automation or JavaScript required | configured repositories=9; repository metadata fetched=9; sampled releases=6; sampled usable release records=6 | usable |
| PyVideo Catalog | Videos | GitHub REST contents API for pyvideo/data conference video JSON documents | GitHub contents directory listing per conference; adapter ingests a bounded deterministic sample, round-robin across conferences | title<br>canonical YouTube URL<br>recorded (talk recording date)<br>description<br>event/publisher<br>AI relevance evidence | recorded is the source-documented date the talk was recorded; it is preserved as video.recorded_at with explicit timestamp semantics and never substituted with crawl/commit time | GitHub REST API returned JSON/base64; no browser automation or JavaScript required | configured conferences=6; listed video files=479; slug-suggested AI candidates=111; sampled files=6; sampled usable video records=6 | usable |
| AI Device Catalog | Devices | GitHub REST contents API for a structured Markdown catalog of embedded boards with AI/ML accelerators (Vge0rge/ai-ml-embedded-boards) | single Markdown catalog document; adapter ingests a bounded deterministic sample | device name<br>canonical vendor URL<br>MCU/processor description<br>AI accelerator evidence | source does not supply per-device timestamps; no release/publication date is fabricated for devices | GitHub REST API returned Markdown as JSON/base64; no browser automation or JavaScript required | parsed entries=21; included board entries=17; excluded (available-later/mcus-only)=4; candidate device entries=17; device-specific entries after URL dedup=16 | usable |\n| Hailo Model Zoo | Devices/Models | GitHub REST git-trees API (recursive) + contents API for hailo-ai/hailo_model_zoo cfg/networks/*.yaml | single repository tree; adapter ingests a bounded deterministic stride sample | network.network_name<br>info.supported_hw_arch | source does not supply per-device/per-model publication timestamps; none is fabricated | GitHub REST API returned YAML as JSON/base64; no browser automation or JavaScript required | network YAMLs=233; with supported_hw_arch=227; device families=3 (Hailo-15H/15L/10H); bounded sample limit=16; yielded 14 models + 3 devices + 37 runs edges | usable |
| Hugging Face Hub API Probe | Models | Hugging Face public models endpoint | UNKNOWN | model id<br>model URL<br>provider/author<br>license<br>modalities<br>timestamps | UNKNOWN | not determined; request failed before content inspection | UNKNOWN | unusable — network: network failure requesting https://huggingface.co/api/models: TLS/SSL connection has been closed (EOF) (_ssl.c:992) |
| OpenAI News RSS Probe | News | official RSS feed URL | UNKNOWN | title<br>URL<br>publication timestamp<br>description/content | publication timestamp required but feed unavailable in this environment | not determined; request failed before content inspection | UNKNOWN | unusable — network: TLS/SSL connection has been closed (EOF) (_ssl.c:992) |
| Y Combinator Companies AI Probe | Startups/Companies | public web page probe | not verified; request failed before page behavior could be inspected | company name<br>company URL<br>description<br>batch/year<br>AI relevance | company profile freshness not verified | not determined; request failed before content inspection | UNKNOWN | unusable — network: TLS/SSL connection has been closed (EOF) (_ssl.c:992) |
| Product Hunt GraphQL Probe | Products | public GraphQL endpoint probe | GraphQL pagination not verified; request failed before schema inspection | product name<br>tagline/description<br>product URL<br>launch/publication date<br>maker/company<br>AI relevance | launch timestamp not verified | not determined; request failed before content inspection | UNKNOWN | unusable — network: TLS/SSL connection has been closed (EOF) (_ssl.c:992) |
| Hacker News Algolia AI Stories Probe | News | HN Algolia search_by_date API probe | page and hitsPerPage parameters expected by endpoint convention; not verified because request failed before response inspection | title<br>URL/story URL<br>created_at story timestamp<br>author<br>objectID<br>AI relevance | created_at would be Hacker News story submission time if reachable; external article publication time is not established by this probe | API-first JSON endpoint expected; no browser automation intended, but accessibility failed before response inspection | UNKNOWN | unusable — network: TLS/SSL connection has been closed (EOF) (_ssl.c:992) |
| Himalayas AI Jobs API Probe | Jobs | Himalayas remote jobs search API probe | offset/limit or page parameters documented by source, but not verified because request failed before response inspection | job title<br>company<br>job URL<br>location<br>posted/created timestamp<br>source | posting timestamp semantics not verified because endpoint was not reachable from this environment | API-first JSON endpoint expected; request failed before content inspection | UNKNOWN | unusable — network: TLS/SSL connection has been closed (EOF) (_ssl.c:992) |
| GDELT AI News API Probe | News | GDELT 2.1 DOC API article list probe | maxrecords/startdatetime/enddatetime parameters are documented by source, but not verified because request failed before response inspection | title<br>canonical article URL<br>source/domain<br>publication timestamp<br>AI relevance | GDELT seendate would need separate validation and is not assumed to be publisher publication time | API-first JSON endpoint expected; request failed before content inspection | UNKNOWN | unusable — network: TLS/SSL connection has been closed (EOF) (_ssl.c:992) |
| Models.dev Model Metadata API Probe | Models | models.dev public model metadata JSON probe | single JSON document expected, but not verified because request failed before response inspection | provider<br>model id<br>modalities or capabilities<br>model URL/source<br>license if available | model metadata freshness not verified because endpoint was not reachable from this environment | API-first JSON endpoint expected; request failed before content inspection | UNKNOWN | unusable — network: TLS/SSL connection has been closed (EOF) (_ssl.c:992) |
| NPM Search AI Packages Probe | Products/Tools | NPM registry search endpoint | size/from search parameters observed by endpoint convention; not used for ingestion in this milestone | package name<br>description<br>version/date<br>repository or homepage<br>maintainer/publisher | package date fields may be update times, not product launch dates | NPM registry JSON endpoint is API-first; no JavaScript required | JSON response contained 5 objects in sampled page | partial — endpoint reachable, but a dedicated adapter must filter packages conservatively before treating them as products/tools |
| OpenRouter Models API Probe | Models | public models endpoint probe | not verified; request failed before response inspection | model id<br>model name<br>provider<br>license<br>modalities<br>model URL | model update/publication timestamps not verified | not determined; request failed before content inspection | UNKNOWN | unusable — network: TLS/SSL connection has been closed (EOF) (_ssl.c:992) |
| TechCrunch AI RSS Probe | News | RSS feed probe | RSS feed page only; historical pagination not verified | title<br>URL<br>publication timestamp<br>description<br>AI category | publication timestamp required but feed unavailable in this environment | not determined; request failed before content inspection | UNKNOWN | unusable — network: TLS/SSL connection has been closed (EOF) (_ssl.c:992) |
| VentureBeat AI RSS Probe | News | RSS feed probe | RSS feed page only; historical pagination not verified | title<br>URL<br>publication timestamp<br>description<br>AI category | publication timestamp required but feed unavailable in this environment | not determined; request failed before content inspection | UNKNOWN | unusable — network: TLS/SSL connection has been closed (EOF) (_ssl.c:992) |
| Remotive AI Jobs API Probe | Jobs | jobs API search endpoint probe | not verified; request failed before response inspection | job title<br>company<br>listing URL<br>location<br>publication timestamp<br>AI relevance | publication timestamp required but endpoint unavailable in this environment | not determined; request failed before content inspection | UNKNOWN | unusable — network: TLS/SSL connection has been closed (EOF) (_ssl.c:992) |
| RemoteOK Jobs API Probe | Jobs | jobs API endpoint probe | not verified; request failed before response inspection | job title<br>company<br>listing URL<br>location<br>publication timestamp<br>tags/AI relevance | publication timestamp required but endpoint unavailable in this environment | not determined; request failed before content inspection | UNKNOWN | unusable — network: TLS/SSL connection has been closed (EOF) (_ssl.c:992) |

## Notes

- `usable` means the endpoint was accessible, parsed, and supplied the required fields for the implemented bounded adapter.
- `partial` means the source showed useful reachability or inventory evidence but was not accepted for ingestion without a stricter production adapter/filter.
- `unusable` means the source was not accessible or parseable enough in this environment to support ingestion.
- Several public web/RSS/API candidates failed at TLS/network connection setup in this environment. Those failures are retained in `data/validation_report.json` and `data/source_feasibility.json`.
- Product records are accepted only from the GitHub-hosted AI Tools List JSON directory sample when handle, website, description, valid URL, explicit AI description evidence, and product/service/platform semantics are present. Provider/company remains `null` because the source does not reliably supply it.
- The product adapter skips observed non-product blurbs such as guides and model-family/company descriptions, and deduplicates root `www` URL variants during discovery.
- Models.dev direct API access still fails from this environment, but the same open-source catalog is live-verified through GitHub REST contents. The Models.dev GitHub adapter adds bounded model records with source-supplied modality/capability metadata and leaves license `null` because the catalog does not provide per-model license fields.
- ROS Robots Catalog records are accepted only from robots.ros.org source posts that identify actual robot systems/components with title, description/introduction/body evidence, and catalog or website/wiki URLs. Software-support blurbs are rejected.
- ROS Robots front-matter dates are preserved as catalog post dates, not robot launch dates.
- GitHub Releases Announcements records are accepted only as News when a release has a valid canonical URL, a parseable `published_at`, a title, and a non-empty release-note body, and only from the curated AI repository allowlist. Draft and prerelease releases are rejected.
- GitHub release `published_at` is the genuine publication timestamp for the announcement; it is never substituted with crawl, retrieval, commit, or page-modification time. `timestamp_semantics` is recorded explicitly, and `fetched_at` remains a separate provenance field.
- `News → published_by → Company` relationships are emitted only when the release repository owner is a GitHub Organization that already resolves to an ingested company entity. OpenAI and Anthropic news records retain publisher metadata but no company relationship because their GitHub orgs supply no company description.
- Hacker News Algolia, GDELT, Himalayas, and Models.dev direct API probes remain recorded as unavailable through their direct endpoints. They are not accepted unless another reachable source path supplies the required fields.
- One-time reachability probes during the News milestone additionally confirmed that Hacker News Firebase API, Hacker News Algolia, news.ycombinator.com, Reddit JSON/API, Lobsters, Wikipedia API, GitHub Blog changelog RSS, RSSHub, raw.githubusercontent.com, and arXiv all fail at TLS connection setup in this environment. Only `api.github.com`, `pypi.org`, and `registry.npmjs.org` returned parseable JSON.
- NPM search package results are accepted only as package/tool records after explicit AI relevance checks and package-specific registry verification; they are not treated as product records.
- PyVideo Catalog records are accepted only as Videos when the source JSON supplies a title, a canonical YouTube URL, a parseable `recorded` date, a non-empty description, and AI relevance evidence from the title/description. Records are accepted only from the curated AI-relevant conference allowlist.
- PyVideo `recorded` is the source-documented date the talk was recorded — a recording date, not a publication/upload timestamp (PyVideo schema: "ISO 8601 Date on which video was recorded"). It is preserved as `video.recorded_at` with `timestamp_semantics = pyvideo_recorded_date` and is never substituted with crawl/retrieval time. PyVideo does not supply a YouTube channel name, so the publisher is recorded as the conference/event, not a fabricated channel identity.
- Videos produce no relationships: the source attributes talks to conferences (no company/developer entity exists for them), and no speculative `published_by`/`runs` edges are manufactured.
- AI Device Catalog records are accepted only from the source's "Available now" and "Boards with other AI/ML Accelerators" sections; "Available later" (announced-only) and "MCUs only" (bare chips) sections are excluded, and two boards sharing one generic catalog URL are deduplicated.
- Device manufacturer is derived only from the board name via a controlled, source-derived token map and recorded with `manufacturer_evidence`; it is `null` when the source does not name a manufacturer.
- The AI Device Catalog (boards) produces no `Device → runs → Model` edges: it documents framework support (TensorFlow/PyTorch), not specific supported model IDs.
- The Hailo Model Zoo supplies the `Device → runs → Model` evidence the boards catalog cannot: each model YAML declares `info.supported_hw_arch` naming the Hailo accelerators that run it. A `runs` edge is created only when a model's own YAML lists the device; the edge records the observed field, observed arch list, the model YAML blob URL, and the reason. Models without `supported_hw_arch` (six legacy face/person models) are excluded. No edge is inferred from framework/CUDA generality or popularity.
- Hailo device identity is source-derived: the official README names Hailo-15H/15L/10H and documents each under `docs/public_models/<DEVICE>/`; `device.manufacturer = Hailo` (repository owner), `device.device_class = ai-accelerator`, and `device.canonical_url` is the device's own documentation directory in the source (the `hailo.ai` product pages are unreachable from this environment, so no unverified product URL is fabricated).
- Compatibility-source investigation (Device → Model): `airockchip/rknn_model_zoo` (RKNN examples lack a single clean per-model hardware field at the probed paths), `qualcomm/ai-hub-models` (device identity is rich via `devices_and_chipsets.yaml`, but per-model device support is expressed through `perf.yaml` benchmark scorecards rather than an explicit supported-device list), and `NVIDIA-AI-IOT/jetson_benchmarks` (benchmark CSVs, not a compatibility catalog) were probed and not selected over the Hailo model zoo's explicit `supported_hw_arch` declarations.
